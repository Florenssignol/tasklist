<%= form_with(model: list_access, url: [@list, list_access], local: true) do |form| %>
  <% if list_access.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(list_access.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
      <% list_access.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.collection_select :user_id, User.order(:name), :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
